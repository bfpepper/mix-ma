<h1><%= @playlist.name %>'s Songs</h1>

<ul>
  <% @playlist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>

<%= link_to "Edit", edit_playlist_path(@playlist) %>
